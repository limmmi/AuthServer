# 安装说明

## 系统要求

- **操作系统**：Linux (Ubuntu 20.04+ / Debian 11+)
- **编译器**：GCC 9+ 或 Clang 10+
- **CMake**：3.10+

## 依赖项

### 1. 安装 OpenSSL 开发库

```bash
sudo apt update
sudo apt install libssl-dev
```

### 2. 安装 CMake (如果版本过低)

```bash
sudo apt install cmake
# 或使用 snap
sudo snap install cmake --classic
```

### 3. 安装 GCC/G++ (如果版本过低)

```bash
sudo apt install build-essential
# 验证版本
gcc --version
g++ --version
```

### 4. 安装 spdlog (日志库)

本项目使用 spdlog 作为日志库，支持 header-only 模式，无需单独编译安装。

**方式一：直接包含源码（推荐）**

```bash
# 在项目根目录执行
git clone https://github.com/gabime/spdlog.git
```

spdlog 源码将自动被 CMake 包含，无需额外配置。

**方式二：系统包管理器安装**

```bash
# Ubuntu/Debian
sudo apt install libspdlog-dev

# 或从源码编译安装
# 详见 spdlog 官方文档
```

## 安装 LevelDB

本项目依赖 `leveldb`，编译步骤如下：

### 1. 克隆源码

```bash
git clone https://github.com/google/leveldb.git
cd leveldb
```

### 2. 编译静态库（推荐）

静态库无需运行时依赖，部署更方便：

```bash
# 清理并创建构建目录
rm -rf build

# 配置（启用异常和RTTI，确保与AuthServer兼容）
cmake -B build -S . \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=../leveldb_install \
    -DLEVELDB_BUILD_TESTS=OFF \
    -DLEVELDB_BUILD_BENCHMARKS=OFF \
    -DCMAKE_CXX_FLAGS="-fexceptions -frtti"

# 编译
cd build
make -j$(nproc)

# 安装
make install
```

### 3. 编译动态库（可选）

如需动态库，添加 `-DBUILD_SHARED_LIBS=ON`：

```bash
# 清理并创建构建目录
rm -rf build

# 配置（动态库）
cmake -B build -S . \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=../leveldb_install \
    -DLEVELDB_BUILD_TESTS=OFF \
    -DLEVELDB_BUILD_BENCHMARKS=OFF \
    -DBUILD_SHARED_LIBS=ON \
    -DCMAKE_CXX_FLAGS="-fexceptions -frtti"

# 编译
cd build
make -j$(nproc)

# 安装
make install
```

**注意**：使用动态库时，运行前需要设置库路径：
```bash
export LD_LIBRARY_PATH=/path/to/leveldb_install/lib:$LD_LIBRARY_PATH
```

### 4. 安装后文件结构

**静态库：**
```
leveldb_install/
├── include/
│   └── leveldb/          # 头文件
├── lib/
│   └── libleveldb.a      # 静态库
└── cmake/leveldb/         # CMake配置
```

**动态库：**
```
leveldb_install/
├── include/
│   └── leveldb/          # 头文件
├── lib/
│   ├── libleveldb.so     # 动态库链接
│   ├── libleveldb.so.1   # 版本链接
│   ├── libleveldb.so.1.23.0  # 实际库文件
│   └── cmake/leveldb/     # CMake配置
```

### 重要：启用异常和 RTTI

LevelDB 默认禁用 C++ 异常和 RTTI，但 AuthServer 需要它们。编译时**必须**添加：
```
-DCMAKE_CXX_FLAGS="-fexceptions -frtti"
```

否则会导致运行时崩溃或数据库打开失败。

## 下载 spdlog 源码

```bash
# 在项目根目录执行
git clone --depth 1 https://github.com/gabime/spdlog.git
```

spdlog 是一个高性能的 C++ 日志库，本项目使用其 header-only 模式，无需编译。

## 编译 AuthServer

```bash
# 回到项目根目录
cd /path/to/AuthServer

# 创建构建目录
rm -rf build
mkdir build
cd build

# 配置
cmake .. -DCMAKE_BUILD_TYPE=Release

# 编译
make -j$(nproc)
```

生成的可执行文件：`build/auth_server`

## 运行

```bash
./build/auth_server
# 或指定参数
./build/auth_server -p 8081 -d ./auth_db -k ./private.key
```

## 常见问题

### 1. 编译 LevelDB 时提示找不到库

确保已安装 OpenSSL：
```bash
sudo apt install libssl-dev
```

### 2. 程序运行时崩溃

确保 LevelDB 和 AuthServer 使用相同的编译选项（不禁用异常和RTTI）。

### 3. 动态库链接失败

运行前设置库路径：
```bash
export LD_LIBRARY_PATH=/path/to/leveldb_install/lib:$LD_LIBRARY_PATH
```
