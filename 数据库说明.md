# AuthServer 数据库说明文档

## 一、数据库概述

本项目使用 **LevelDB** 作为存储引擎，采用键值对（Key-Value）存储模式，没有传统关系型数据库的表结构。

- **数据库类型**: LevelDB（Google 开源键值数据库）
- **存储格式**: 键值对（Key-Value）
- **数据路径**: 默认 `./auth_db/`
- **编码格式**: UTF-8

---

## 二、数据存储结构

### 2.1 键值对格式

| 键（Key） | 值（Value） | 说明 |
|-----------|-------------|------|
| `用户名` | `userId:role:passwordHash` | 用户数据 |
| `__max_user_id__` | `数字字符串` | 当前最大用户ID计数器 |

### 2.2 Value 字段详解

Value 使用冒号 `:` 分隔三个字段：

```
userId:role:passwordHash
  │      │       │
  │      │       └── 密码哈希（PBKDF2 算法）
  │      └── 用户角色（admin / normal）
  └── 用户唯一ID（自增数字）
```

### 2.3 实际数据示例

```
┌─────────────────────────────────────────────────────────────┐
│ Key: "admin"                                                │
│ Value: "1:admin:$pbkdf2$12$c2FsdA==$abc123..."              │
│      │  │    │                                              │
│      │  │    └── 密码哈希                                   │
│      │  └────── 角色：管理员                                 │
│      └───────── 用户ID：1                                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Key: "zhangsan"                                             │
│ Value: "2:normal:$pbkdf2$12$dXNlcg==$xyz789..."             │
│      │  │     │                                             │
│      │  │     └── 密码哈希                                  │
│      │  └─────── 角色：普通用户                              │
│      └────────── 用户ID：2                                   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Key: "__max_user_id__"                                      │
│ Value: "2"                                                  │
│        └── 当前最大用户ID，用于生成下一个ID                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、用户角色系统

### 3.1 角色定义

| 角色值 | 说明 | 权限 |
|--------|------|------|
| `admin` | 管理员 | 可创建新用户，可指定用户角色 |
| `normal` | 普通用户 | 仅能登录，无法创建其他用户 |

### 3.2 角色分配规则

1. **第一个注册用户**自动成为 `admin`
2. **普通注册接口**创建的用户默认为 `normal`
3. **管理员接口**可指定创建 `admin` 或 `normal` 用户

---

## 四、API 接口说明

### 4.1 基础信息

- **服务器地址**: `http://localhost:8081`
- **请求格式**: JSON
- **响应格式**: JSON

### 4.2 接口列表

#### 1. 用户注册（普通注册）

```http
POST /api/register
Content-Type: application/json
```

**请求参数**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 用户名（3-32字符） |
| password | string | 是 | 密码（6-128字符） |

**响应示例**:

```json
{
  "success": "true",
  "message": "User registered successfully"
}
```

**特殊说明**: 第一个注册的用户自动成为管理员。

---

#### 2. 用户登录

```http
POST /api/login
Content-Type: application/json
```

**请求参数**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

**响应示例**（成功）:

```json
{
  "success": "true",
  "token": "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...",
  "role": "admin",
  "userId": "1"
}
```

**响应示例**（失败）:

```json
{
  "error": "Invalid username or password"
}
```

**JWT Token 说明**:
- Token 包含用户ID和角色信息
- 默认有效期：24小时（86400秒）
- 使用 RS256 非对称加密签名

---

#### 3. 管理员注册（需管理员权限）

```http
POST /api/admin/register
Content-Type: application/json
```

**请求参数**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 用户名（3-32字符） |
| password | string | 是 | 密码（6-128字符） |
| role | string | 是 | 角色：`admin` 或 `normal` |
| admin_token | string | 是 | 管理员 JWT Token |

**响应示例**（成功）:

```json
{
  "success": "true",
  "message": "User registered successfully",
  "role": "normal"
}
```

**响应示例**（无权限）:

```json
{
  "error": "Registration failed (not admin, user exists or invalid input)"
}
```

---

#### 4. 查询用户信息

```http
GET /api/user/info?username={用户名}
```

**请求参数**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 查询的用户名（URL参数） |

**响应示例**（成功）:

```json
{
  "username": "zhangsan",
  "userId": "2",
  "role": "normal"
}
```

**响应示例**（用户不存在）:

```json
{
  "error": "User not found"
}
```

---

#### 5. 健康检查

```http
GET /api/health
```

**响应示例**:

```json
{
  "status": "ok",
  "service": "auth_server"
}
```

---

## 五、使用示例

### 5.1 完整流程示例

```bash
# 1. 启动服务器
./build/auth_server -p 8081 -k ./private.key -d ./auth_db

# 2. 注册第一个管理员
curl -X POST http://localhost:8081/api/register \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
# 返回：{"success":"true",...} 第一个用户自动成为 admin

# 3. 管理员登录获取 token
curl -X POST http://localhost:8081/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
# 返回：{"token":"eyJ...","role":"admin","userId":"1"}

# 4. 管理员创建普通用户
curl -X POST http://localhost:8081/api/admin/register \
  -H "Content-Type: application/json" \
  -d '{
    "username":"user1",
    "password":"user123",
    "role":"normal",
    "admin_token":"eyJ..."
  }'

# 5. 管理员创建另一个管理员
curl -X POST http://localhost:8081/api/admin/register \
  -H "Content-Type: application/json" \
  -d '{
    "username":"admin2",
    "password":"admin456",
    "role":"admin",
    "admin_token":"eyJ..."
  }'

# 6. 查询用户信息
curl "http://localhost:8081/api/user/info?username=user1"
# 返回：{"username":"user1","userId":"2","role":"normal"}
```

---

## 六、安全说明

1. **密码存储**: 使用 PBKDF2 算法哈希，不可逆加密
2. **私钥保护**: RSA 私钥从外部文件加载，不硬编码在代码中
3. **Token 安全**: JWT 使用 RS256 非对称签名，业务服务器只需公钥验证
4. **权限控制**: 敏感操作（创建用户）需管理员 Token 验证

---

## 七、数据库备份

LevelDB 数据存储在文件系统中，可直接备份数据目录：

```bash
# 备份
cp -r auth_db auth_db_backup_$(date +%Y%m%d)

# 恢复
cp -r auth_db_backup_20240101 auth_db
```

**注意**: 备份前建议先停止服务器，确保数据一致性。
