# 日志系统说明

## 概述

AuthServer 使用 [spdlog](https://github.com/gabime/spdlog) 作为日志库，支持控制台彩色输出和文件日志记录。

## 日志配置

### 输出目标

| 目标 | 级别 | 说明 |
|------|------|------|
| 控制台 | `info` 及以上 | 带颜色输出（白色info/黄色warn/红色error） |
| 文件 | `debug` 及以上 | 轮转日志，单文件最大10MB，保留3个备份 |

### 日志格式

```
[2024-01-15 10:30:25.123] [info] 日志消息内容
[2024-01-15 10:30:25.124] [warn] 警告消息
[2024-01-15 10:30:25.125] [error] 错误消息
```

### 日志文件位置

默认路径：`./logs/auth_server.log`

## 日志内容分类

### 1. 系统生命周期日志

| 事件 | 级别 | 示例 |
|------|------|------|
| 服务器启动 | info | `Auth Server Starting...` |
| 配置信息 | info | `Port: 8081, Database: ./auth_db` |
| 服务初始化 | info | `AuthService initialized, next user ID: 1` |
| 信号接收 | info | `Received signal 2, shutting down...` |
| 服务器停止 | info | `Server stopped.` |

### 2. 网络连接日志

| 事件 | 级别 | 示例 |
|------|------|------|
| 新客户端连接 | info | `New client connected: 192.168.1.100:54321` |
| 客户端断开 | info | `Client disconnected: 192.168.1.100:54321` |
| 连接错误 | error | `accept error: Too many open files` |
| epoll错误 | error | `Failed to add client to epoll ...` |

### 3. HTTP请求日志

| 事件 | 级别 | 示例 |
|------|------|------|
| 请求记录 | info | `POST /api/login from client` |
| 未知请求 | warn | `Unknown request: GET /api/unknown` |
| 参数缺失 | warn | `Login request missing username or password` |

### 4. 用户认证日志

#### 登录操作

| 结果 | 级别 | 示例 |
|------|------|------|
| 登录成功 | info | `Login success: alice (role: admin)` |
| 用户不存在 | warn | `Login failed: user not found - bob` |
| 密码错误 | warn | `Login failed: wrong password - alice` |
| 参数为空 | warn | `Login failed: empty username or password` |

#### 注册操作

| 结果 | 级别 | 示例 |
|------|------|------|
| 注册成功 | info | `User registered: bob (role: normal, id: 2)` |
| 首用户管理员 | info | `First user registered as ADMIN: admin` |
| 用户已存在 | warn | `Register failed: user already exists - bob` |
| 用户名长度无效 | warn | `Register failed: username length invalid - ab` |
| 密码长度无效 | warn | `Register failed: password length invalid - bob` |
| 密码哈希失败 | error | `Register failed: password hash failed - bob` |
| 数据库错误 | error | `Register failed: database error - bob` |

#### 管理员注册

| 结果 | 级别 | 示例 |
|------|------|------|
| 注册成功 | info | `Admin registered new user: charlie (role: admin, id: 3)` |
| Token无效 | warn | `Admin register failed: invalid admin token` |
| 其他失败 | warn/error | 同普通注册 |

### 5. 数据库操作日志

| 事件 | 级别 | 示例 |
|------|------|------|
| 打开失败 | error | `Failed to open database at: ./auth_db` |
| Put失败 | error | `Database Put failed for key 'user': IO error` |
| Delete成功 | info | `Database key deleted: old_user` |

### 6. 安全相关日志

| 事件 | 级别 | 说明 |
|------|------|------|
| 私钥加载失败 | error | 启动时无法读取私钥文件 |
| 认证服务初始化失败 | error | 数据库或密钥问题 |
| 日志系统初始化失败 | error | 文件权限或磁盘空间问题 |

## 日志级别说明

| 级别 | 颜色 | 用途 |
|------|------|------|
| trace | 白 | 最详细的调试信息（未使用） |
| debug | 白 | 调试信息（仅记录到文件） |
| info | 绿/白 | 正常操作记录 |
| warn | 黄 | 警告，操作可能有问题 |
| error | 红 | 错误，操作失败 |
| critical | 红/粗 | 严重错误（未使用） |

## 查看日志

### 实时查看控制台输出

```bash
./build/auth_server
```

### 查看日志文件

```bash
# 查看最新日志
tail -f logs/auth_server.log

# 查看所有日志
cat logs/auth_server.log

# 搜索特定用户
 grep "alice" logs/auth_server.log

# 搜索错误
 grep "\[error\]" logs/auth_server.log
```

### 日志轮转

当日志文件达到 10MB 时，自动创建备份：
```
logs/
├── auth_server.log          # 当前日志
├── auth_server.log.1        # 上一个备份
├── auth_server.log.2        # 更旧的备份
└── auth_server.log.3        # 最旧的备份
```

## 故障排查

### 登录失败排查

1. 查看 `Login failed` 日志确定原因
2. 检查用户名是否存在
3. 检查密码是否正确

### 注册失败排查

1. 查看 `Register failed` 日志
2. 检查用户名/密码长度要求
3. 检查用户是否已存在

### 数据库错误排查

1. 检查磁盘空间
2. 检查文件权限
3. 查看 `Database` 相关错误日志
